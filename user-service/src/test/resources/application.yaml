# disable external services
spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  liquibase:
    enabled: true
    change-log: classpath:migrations/master.xml

api:
  server:
    url: http://localhost:8080